# Basic Login Form

---

## Install MongoDB Globally

```bash
npm install -g mongodb
```

## Start MongoDB

In a different terminal window, start the `mongod` daemon

```bash
sudo mongod
```

## Start the app

```bash
npm run start
```
